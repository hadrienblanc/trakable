# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in trakable.gemspec
gemspec

group :development, :test do
  gem 'debug', platforms: %i[mri windows]
end
